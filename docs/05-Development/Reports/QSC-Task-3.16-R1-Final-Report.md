# QSC Task 3.16-R1 — PostgreSQL Verification, Product Entry Live Integration and Currency Registry Hardening — Final Report

## Status

`ReadyForReview`. Every required gate passed, including a guarded clean PostgreSQL migration from `0000` through `0012` and 102/102 live assertions. Independent review is still required. | الحالة `ReadyForReview`. نجحت جميع البوابات المطلوبة، بما فيها ترحيل PostgreSQL المحروس والنظيف من `0000` إلى `0012` ونجاح 102 من 102 اختبارًا حيًا. ما زالت المراجعة المستقلة مطلوبة.

## Task

Task 3.16-R1 — PostgreSQL Verification, Product Entry Live Integration and Currency Registry Hardening. | المهمة 3.16-R1 — التحقق من PostgreSQL وربط Product Entry بالبيانات الحية وتقوية سجل العملات.

## Branch

`feature/catalog-reference-data`

## English Summary

R1 closes the three independent-review blockers without redesigning Task 3.16. The repository-defined PostgreSQL 17 service was started, the safety guard accepted only the dedicated test target, both application and migration-ledger schemas were reset, and the canonical migrator applied `0000` through `0012` before all 102 assertions passed. Production Product Entry now composes `HttpProductEntryCatalogReferenceDataClient` through a Presentation coordinator and uses the active Workspace read model for hierarchy, Brand, Device Class, Condition, Supply Status, Currency, and Product Type Specification Template selections. Currency identity is a fixed complete ISO 4217 List One registry; Workspace storage remains enablement/order only, PostgreSQL validates alpha-3 shape, and Application validates registry membership.

## Arabic Summary

يعالج R1 عوائق المراجعة المستقلة الثلاثة دون إعادة تصميم المهمة 3.16. شُغلت خدمة PostgreSQL 17 المعرفة في المستودع، وقبل حاجز الأمان قاعدة الاختبار المخصصة فقط، ثم أُعيد ضبط مخططي التطبيق وسجل الترحيلات وطبق المهاجر المعتمد السلسلة من `0000` إلى `0012` قبل نجاح جميع الاختبارات الحية وعددها 102. يركّب Product Entry في الإنتاج الآن `HttpProductEntryCatalogReferenceDataClient` عبر منسق في طبقة العرض، ويستخدم نموذج القراءة النشط لمساحة العمل للتسلسل والعلامة التجارية وفئة الجهاز والحالة وحالة التوريد والعملة وقالب مواصفات نوع المنتج. أصبحت هوية العملة سجلًا ثابتًا وكاملًا من قائمة ISO 4217 الأولى؛ وتحفظ مساحة العمل الإتاحة والترتيب فقط، بينما تتحقق PostgreSQL من شكل الرمز وتتحقق طبقة التطبيق من عضويته في السجل.

## Independent Review Findings Addressed

- PostgreSQL verification now reaches and passes all assertions on a clean guarded database. | يصل تحقق PostgreSQL الآن إلى جميع الاختبارات وينجح من قاعدة نظيفة ومحروسة.
- The previously unused Product Entry reference-data port is now the production composition source; there is no production reference-data mock fallback. | أصبح منفذ البيانات المرجعية هو مصدر التركيب الإنتاجي ولا يوجد رجوع إنتاجي إلى بيانات مرجعية وهمية.
- USD/YER/SAR are no longer treated as the complete currency invariant in TypeScript or PostgreSQL. | لم تعد USD وYER وSAR تمثل تعريف العملة الكامل في TypeScript أو PostgreSQL.

## PostgreSQL Environment Review

Docker Desktop and the repository-defined PostgreSQL container were initially stopped and were started only for the approved local workflow. `TEST_DATABASE_URL` passed the existing safety guard against `DATABASE_URL`; neither URL nor credentials were printed. Only the dedicated `qsc_test` database was reset. No Production migration was run. | كانت Docker Desktop وحاوية PostgreSQL متوقفتين، وشُغّلتا لمسار العمل المحلي المعتمد فقط. نجح رابط الاختبار في حاجز الأمان القائم مقابل رابط التطبيق دون طباعة الروابط أو بيانات الاعتماد. أُعيد ضبط قاعدة `qsc_test` المخصصة فقط ولم يُنفذ أي ترحيل إنتاجي.

## Clean Migration Chain Review

The final clean run removed only `qsc_test` application and Drizzle ledger schemas, recreated `public`, then `npm.cmd run test:integration` applied the canonical chain `0000` → … → `0010` → `0011` → `0012`. All 102 assertions in 19 suites passed. | أزال التشغيل النهائي النظيف مخططي التطبيق وسجل Drizzle من `qsc_test` فقط، ثم أعاد إنشاء `public` وطبق الأمر المعتمد السلسلة كاملة. نجحت 102 من 102 اختبارًا ضمن 19 حزمة.

## Migration 0012 Review

The uncommitted `0012_catalog_reference_data.sql` remains the sole Task 3.16 migration. Its currency constraint now accepts only uppercase alpha-3 storage shape. Migrations `0000`–`0011` were not edited. A focused SQL review found zero duplicate Product Media Source Attempt statements from SQL-only migration `0011`. | بقي `0012_catalog_reference_data.sql` ترحيل المهمة الوحيد. يقبل قيد العملة شكلًا من ثلاثة أحرف كبيرة فقط. لم تُعدل الترحيلات `0000`–`0011`، وأثبت الفحص عدم تكرار عبارات Product Media المملوكة للترحيل `0011`.

## Drizzle Metadata Review

`0012_snapshot.json` was regenerated by `drizzle-kit`; it was not fabricated or hand-edited. `_journal.json` contains index 12 and tag `0012_catalog_reference_data`. `npm.cmd run db:check` reports `Everything's fine`. The snapshot intentionally represents the complete schema after SQL-only `0011`, while the SQL migration contains only the new Task 3.16 delta. | أُعيد توليد لقطة `0012` بأداة Drizzle ولم تُكتب يدويًا. يحتوي السجل الفهرس 12 والوسم الصحيح، ونجح فحص Drizzle. تمثل اللقطة المخطط الكامل بعد `0011` المكتوب بـSQL، بينما يحتوي ملف `0012` على فرق المهمة الجديدة فقط.

## Currency Registry Review

The Domain contains the complete 178-code current ISO 4217 List One currency/fund registry published by the official ISO Maintenance Agency, SIX, as reviewed on 2026-08-19. Workspace rows contain code, enablement, and sort order only. PostgreSQL enforces `^[A-Z]{3}$`; Application rejects any shape-valid value not in the fixed registry (for example `ZZZ`). A legitimate registry update changes code, tests, and documentation without redesigning Workspace persistence or adding a new schema concept. No rates, conversion, or pricing logic was added. | يحتوي المجال على السجل الحالي الكامل المؤلف من 178 رمزًا من قائمة ISO 4217 الأولى المنشورة من جهة الصيانة الرسمية SIX وفق مراجعة 2026-08-19. تحفظ مساحة العمل الرمز والإتاحة والترتيب فقط. تفرض PostgreSQL شكل الرمز، وترفض طبقة التطبيق أي رمز غير عضو مثل `ZZZ`. لا توجد أسعار صرف أو تحويل أو منطق تسعير.

## Condition Compatibility Review

`new`, `used`, and `refurbished` remain the only fixed Condition identities. `refurbished` is explicitly retained as a compatibility value in the Product/Product Entry type contract and is never Workspace-created. | تبقى `new` و`used` و`refurbished` هويات الحالة الثابتة الوحيدة. حُفظت `refurbished` صراحةً للتوافق في عقد Product وProduct Entry ولا تنشئها مساحة العمل.

## Product Entry Live Integration Review

Production creates `ProductEntryCatalogReferenceDataCoordinator` with `HttpProductEntryCatalogReferenceDataClient`. The HTTP GET carries only normal request metadata and no Workspace/Actor business authority; Trusted Session remains the server authority. The wizard consumes Departments, Categories, Product Types, Brands, fixed Device Classes, enabled Conditions, dynamic Supply Statuses, enabled ISO Currencies, Specification Definitions, and Templates from the returned active Workspace model. API failure stops Product Entry safely; an empty active model shows setup-required state. | ينشئ الإنتاج المنسق باستخدام محول HTTP القائم. لا يرسل طلب المتصفح هوية مساحة العمل أو الممثل، وتبقى الجلسة الموثوقة سلطة الخادم. يستهلك المعالج جميع القيم المرجعية المطلوبة من نموذج مساحة العمل النشط، ويفشل بأمان عند تعذر الواجهة ويظهر احتياج الإعداد عند خلو البيانات.

## Legacy Mock Removal/Isolation Review

Legacy reference-data services no longer feed the production Department, Category, Product Type, Brand, Device Class, Condition, Supply Status, Currency, or Specification selectors. Their fixtures remain available to existing unit tests and the pre-existing Product Model boundary. Product Model itself was not added to the Task 3.16 Reference Data model and was not redesigned in R1. No production fallback invents Task 3.16 reference values. | لم تعد الخدمات المرجعية القديمة تغذي محددات المهمة في الإنتاج. تبقى fixtures للاختبارات وحد Product Model السابق، إذ لم يكن Product Model جزءًا من نموذج البيانات المرجعية للمهمة ولم يُعد تصميمه في R1. لا يخترع الإنتاج قيمًا مرجعية بديلة.

## Hierarchy Selection Review

Presentation coordination filters Categories by server-authored `departmentId` and Product Types by server-authored `categoryId`. Changing Department clears an incompatible Category and Product Type; changing Category clears an incompatible Product Type. React delegates reconciliation to the coordinator/workflow and contains no tenant or hierarchy authority. | ترشح طبقة التنسيق الفئات حسب `departmentId` وأنواع المنتجات حسب `categoryId` القادمين من الخادم. يمسح تغيير القسم الفئة ونوع المنتج غير المتوافقين، ويمسح تغيير الفئة نوع المنتج غير المتوافق. لا تملك React سلطة المستأجر أو قواعد التسلسل.

## Specification Template Integration Review

Selecting a Product Type resolves its one active template and projects the ordered active Specification Definitions into new-entry fields. Missing templates remain explicit, and existing persisted Product specification values are never rewritten. | يحل اختيار نوع المنتج قالبَه النشط الوحيد ويعرض تعريفات المواصفات النشطة والمرتبة كحقول لإدخال جديد. يبقى غياب القالب صريحًا ولا يعاد كتابة قيم المنتجات المحفوظة.

## HTTP Boundary Review

Seven focused route-handler tests cover unauthenticated 401, restricted 403, read/manage 403 mapping, `includeInactive`, disallowed mutation origin, malformed DTO 400 without use-case invocation, foreign parent 404, stale 409, persistence uniqueness 409, and successful read/create/update/configuration status mapping. Next.js internals are not tested. | تغطي سبعة اختبارات مركزة المصادقة والجلسة المقيدة وصلاحيات القراءة والإدارة والأصل غير المسموح وDTO غير الصالح والأب الأجنبي والتعارض القديم وتعارض التفرد وربط حالات النجاح، دون اختبار تفاصيل Next.js الداخلية.

## Architecture Review

DDD, Clean Architecture, and the Modular Monolith remain intact. Domain owns registries and invariants; Application owns permissions, transactions, audit, and registry membership; Infrastructure owns PostgreSQL and HTTP; Presentation owns selection coordination; React renders the view model. No dependency was added and no database call moved into a component. | بقيت DDD وClean Architecture والبنية الوحدوية سليمة. يملك المجال السجلات والثوابت، ويملك التطبيق الصلاحيات والمعاملات والتدقيق، وتملك البنية التحتية PostgreSQL وHTTP، وتملك طبقة العرض تنسيق الاختيار، وتعرض React نموذج القراءة. لم تُضف مكتبة ولم ينتقل اتصال قاعدة بيانات إلى مكون.

## Multi-Tenant Review

Every owned lookup remains Workspace-scoped. Composite foreign keys reject cross-Workspace Department/Category/Product Type/Definition/Template relationships. Live tests prove foreign reads, updates, and parents are hidden or rejected and the same code may exist in different Workspaces. | تبقى كل قراءة مملوكة مقيدة بمساحة العمل، وتمنع المفاتيح المركبة العلاقات العابرة للمستأجرين. تثبت الاختبارات الحية إخفاء أو رفض القراءات والتحديثات والآباء الأجانب والسماح بالرمز نفسه في مساحات مختلفة.

## Authorization Review

`catalog.referenceData.view` is required for active reads; `catalog.referenceData.manage` is additionally required for inactive reads and mutations. Migrated Identity constraints accept both permission codes. Restricted or unauthenticated sessions fail closed. | تلزم صلاحية العرض للقراءة النشطة وتلزم صلاحية الإدارة للقراءة غير النشطة والكتابة. تقبل قيود Identity المرحّلة الرمزين، وتفشل الجلسات المقيدة أو غير المصادق عليها بأمان.

## Transaction Review

Reference writes and their Audit append remain inside the Application-owned unit-of-work transaction. Repositories do not call repositories. Template replacement and registry availability configuration are atomic. | تبقى كتابة المراجع وسجل التدقيق داخل معاملة وحدة العمل المملوكة للتطبيق، ولا يستدعي مستودع مستودعًا آخر. استبدال القالب وإعداد الإتاحة عمليتان ذريتان.

## Concurrency Review

Dynamic records and templates use optimistic versions. Live coverage proves stale Department and Template versions return typed conflicts while one stable template identity remains per Product Type. Unique indexes remain the final concurrent enforcement layer. | تستخدم السجلات والقوالب إصدارات تفاؤلية. تثبت التغطية الحية رفض الإصدارات القديمة مع بقاء هوية قالب واحدة لكل نوع منتج، وتبقى الفهارس الفريدة طبقة الإنفاذ النهائية للتزامن.

## Audit Review

Existing create, update, lifecycle, availability, and template use cases continue to append actor-scoped Audit events in the same transaction. R1 did not weaken or bypass Audit behavior. | تستمر حالات الاستخدام في إضافة أحداث التدقيق المقيدة بالممثل داخل المعاملة نفسها، ولم يضعف R1 هذا السلوك أو يتجاوزه.

## Security Review

No environment file, URL, credential, token, password, customer data, or database dump is included. HTTP errors remain sanitized. Mutations enforce same origin before trusted-context resolution, and browser payloads contain no Workspace/Actor authority. | لا تتضمن الأدلة ملفات بيئة أو روابط اعتماد أو رموزًا أو كلمات مرور أو بيانات عملاء أو نسخ قواعد بيانات. تبقى أخطاء HTTP منقحة، وتتحقق الكتابات من الأصل قبل حل السياق، ولا تحمل رسائل المتصفح سلطة مساحة العمل أو الممثل.

## Test Results

- `npx.cmd tsc --noEmit` — passed. | نجح.
- `npx.cmd tsc --noEmit -p tsconfig.integration.json` — passed. | نجح.
- `npm.cmd run lint` — passed. | نجح.
- `npm.cmd test` — passed, including all nested Task Review, Product Media, Product Entry, Reference Data, and Identity suites. | نجح مع جميع الحزم الفرعية.
- `npm.cmd run test:reference-data` — passed 14/14, including 7 HTTP boundary tests. | نجح 14/14.
- `npm.cmd run test:product-entry` — passed 139/139. | نجح 139/139.
- `npm.cmd run test:product-media` — passed 108, with one existing platform-permission skip and zero failures. | نجح 108 مع تخطٍ واحد متعلق بصلاحية المنصة ودون فشل.
- `npm.cmd run build` — passed; 35 static pages generated and all dynamic routes compiled. | نجح البناء.
- `npm.cmd run db:check` — passed. | نجح فحص Drizzle.
- `git diff --check` — passed. `git status --short` and `git diff --stat` were captured read-only. | نجح فحص الفروقات وسُجلت الحالة والإحصاءات للقراءة فقط.
- `npm audit`, `npm audit --omit=dev`, and `npm audit fix` — intentionally not run per task instruction. | لم تُشغل أوامر التدقيق وفق تعليمات المهمة.

## PostgreSQL Integration Results

`npm.cmd run test:integration` passed 102/102 across 19 suites after the final clean guarded reset and full `0000`–`0012` migration. Tests reached hierarchy, uniqueness, lifecycle, fixed registries, Specification Definition/Template, tenant composite FK, permission constraint, concurrency, Identity, Product, Product Entry, and Product Media assertions. | نجح الأمر 102/102 ضمن 19 حزمة بعد إعادة الضبط النظيفة والترحيل الكامل، ووصلت الاختبارات إلى جميع التأكيدات المطلوبة.

## Product Entry Results

139/139 focused Product Entry tests passed. New coverage proves the production factory instantiates the HTTP client, active data loads, hierarchy filters, incompatible descendants clear, Brand/Condition/Currency/Supply Status come from the live contract, templates resolve, unavailable responses fail closed, no mock fallback is returned, and the browser request carries no Workspace/Actor authority. | نجح 139/139 اختبارًا مركزًا، وتثبت التغطية التركيب الإنتاجي والتحميل والترشيح والمسح ومصادر القيم والقالب والفشل الآمن وغياب الرجوع الوهمي وسلطة المتصفح.

## HTTP Boundary Results

All 7 focused Reference Data route-handler boundary tests passed as part of the 14/14 Reference Data suite. | نجحت اختبارات حدود HTTP السبعة ضمن حزمة البيانات المرجعية 14/14.

## Files Created

- `domains/catalog/product-entry/components/steps/CatalogHierarchyStep.tsx`
- `domains/catalog/product-entry/infrastructure/browser/product-entry-catalog-reference-data.composition.ts`
- `domains/catalog/product-entry/infrastructure/product-entry-catalog-reference-data.composition.test.ts`
- `domains/catalog/product-entry/presentation/product-entry-catalog-reference-data.coordinator.ts`
- `domains/catalog/reference-data/infrastructure/http/catalog-reference-data-route-handlers.test.ts`
- `docs/05-Development/Reports/QSC-Task-3.16-R1-Final-Report.md`

These are the R1-specific new files; the uncommitted Task 3.16 foundation files remain present and are included in the review bundle. | هذه ملفات R1 الجديدة تحديدًا، وتبقى ملفات أساس المهمة 3.16 غير الملتزمة موجودة ومضمنة في حزمة المراجعة.

## Files Modified

- Product Entry composition/presentation: `ProductEntryWizard.tsx`, `ProductEntryStepContent.tsx`, `ProductModelStep.tsx`, `CommercialDetailsStep.tsx`, workflow context, reconciliation, validation, commercial options, Product condition type, port contract, and focused Presentation tests.
- Reference Data: Domain currency registry/tests, PostgreSQL integration coverage, and architecture documentation.
- Migration/tooling: Catalog persistence schema, `0012_catalog_reference_data.sql`, generated `0012_snapshot.json`, `_journal.json`, `package.json`, and the existing integration TypeScript inclusion.

عدّل R1 تركيب وعرض Product Entry وعقد المنفذ وسجل العملات واختبارات PostgreSQL ووثيقة المعمارية وترحيل `0012` وبيانات Drizzle وأوامر الاختبار فقط.

## Files Deleted

None. | لا يوجد.

## Files Intentionally Unchanged

Migrations `0000`–`0011`; Task E and Task 3.15.2 implementation; existing persisted Product rows/specification values; Inventory, Pricing, Search, Sharing, Product Media, and Identity architecture; management UI; starter/reference seed data; and the original Task 3.16 final report were preserved. | حُفظت الترحيلات السابقة وتنفيذات المهام السابقة والمنتجات المحفوظة والمجالات الأخرى وواجهة الإدارة وبيانات البداية والتقرير الأصلي دون تعديل مقصود في R1.

## Known Limitations

- Dedicated Reference Data management Presentation is not included; typed management HTTP/use-case boundaries remain the intentional Task 3.16 surface. | لا توجد واجهة إدارة مخصصة؛ تبقى حدود HTTP وحالات الاستخدام هي السطح المقصود.
- No Workspace starter data is fabricated, so an unconfigured Workspace intentionally sees setup-required state. | لا تُختلق بيانات بداية وتظهر حالة احتياج الإعداد للمساحة غير المهيأة.
- Product Model is an existing boundary outside the Task 3.16 Reference Data model; R1 does not redesign or persist Product Models. | Product Model حد قائم خارج نموذج المهمة ولم يُعد تصميمه.
- ISO registry changes still require a reviewed code/data update, but no Workspace schema redesign or migration concept is required. | يتطلب تحديث معيار ISO تحديثًا مراجعًا للكود/البيانات، لا إعادة تصميم لمخطط مساحة العمل.

## Required Confirmations

An independent reviewer must confirm the live Product Entry composition, hierarchy clearing, empty/unavailable states, ISO registry contract, generated snapshot lineage, SQL-only `0011` preservation, clean PostgreSQL evidence, permissions, tenant isolation, and DEV-001 bundle. This report is not self-approval. | يجب على مراجع مستقل تأكيد التركيب الحي والمسح وحالات الخلو والتعذر وعقد ISO وسلسلة اللقطة وحفظ `0011` وأدلة PostgreSQL والصلاحيات والعزل والحزمة. هذا التقرير ليس اعتمادًا ذاتيًا.

## Git and Review Integrity

No Git write command was run: no checkout, switch, reset, restore, clean, stash, add, commit, merge, rebase, push, tag, or branch deletion. The branch remains `feature/catalog-reference-data`. Source files are preserved byte-exact in the review bundle; generated evidence alone is sanitized. Expected artifact paths are:

- Repository report: `docs/05-Development/Reports/QSC-Task-3.16-R1-Final-Report.md`
- Repository ZIP: `artifacts/task-reviews/QSC-Task-3.16-R1-Review.zip`
- Repository checksum: `artifacts/task-reviews/QSC-Task-3.16-R1-Review.zip.sha256`
- Desktop report/ZIP/checksum: `C:\Users\dell\Desktop\QSC-Reviews\QSC-Task-3.16-R1-*`

لم يُنفذ أي أمر كتابة Git، وبقي الفرع الصحيح. تحفظ الحزمة ملفات المصدر كما هي وتنقح الأدلة المولدة فقط.

## Summary

Task 3.16-R1 is implementation-complete and `ReadyForReview`: PostgreSQL passes cleanly, Product Entry consumes live Workspace Reference Data through the typed HTTP composition, and the currency contract no longer makes USD/YER/SAR schema invariants. | اكتمل تنفيذ R1 وهو `ReadyForReview`: نجح PostgreSQL من قاعدة نظيفة، ويستهلك Product Entry البيانات المرجعية الحية عبر HTTP المكتوب بالأنواع، ولم تعد العملات الثلاث قيدًا بنيويًا.

## Next Recommendation

Review the R1 report and DEV-001 archive independently. If approved, commit and push Task 3.16, wait for GitHub Actions, merge, and only then begin Task 3.17. | راجع التقرير والحزمة مستقلًا. بعد الاعتماد فقط، التزم وادفع المهمة وانتظر GitHub Actions ثم ادمجها، وبعد ذلك ابدأ المهمة 3.17.
