export interface SpecificationOptionSetEntity {
  id: string;
  code: string;
  companyId: string;
  workspaceId: string;
  name: string;
  label: string;
  description?: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
}
